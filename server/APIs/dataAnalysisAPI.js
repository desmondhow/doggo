const express = require('express');
const router = express.Router();


// GET route for reading data
router.get('/', function (req, res, next) {
    res.send('Data Analysis API not IMPL');
});

module.exports = router;
